// ============================================
//   AFM Koinonia Centre — JavaScript
//   script.js
// ============================================

// ── MOBILE MENU TOGGLE ──
function toggleMenu() {
  document.getElementById('navbar').classList.toggle('open');
}

// Close mobile menu when any nav link is clicked
document.querySelectorAll('.nav-links a').forEach(function(link) {
  link.addEventListener('click', function() {
    document.getElementById('navbar').classList.remove('open');
  });
});

// ── SCROLL REVEAL ──
// Animates elements with the class "reveal" into view as you scroll
const reveals = document.querySelectorAll('.reveal');

const revealObserver = new IntersectionObserver(function(entries) {
  entries.forEach(function(entry, index) {
    if (entry.isIntersecting) {
      setTimeout(function() {
        entry.target.classList.add('visible');
      }, index * 80);
      revealObserver.unobserve(entry.target);
    }
  });
}, { threshold: 0.12 });

reveals.forEach(function(el) {
  revealObserver.observe(el);
});

// ── NAV BACKGROUND ON SCROLL ──
// Makes the navbar more opaque once user scrolls down
window.addEventListener('scroll', function() {
  var navbar = document.getElementById('navbar');
  if (window.scrollY > 60) {
    navbar.style.background = 'rgba(13,13,13,0.97)';
  } else {
    navbar.style.background = 'rgba(13,13,13,0.85)';
  }
});